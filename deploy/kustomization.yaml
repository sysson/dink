apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - dink.yaml

configMapGenerator:
  - name: dink-config
    namespace: dink-system
    files:
      - config.json
    options:
      labels:
        app.kubernetes.io/name: dink
